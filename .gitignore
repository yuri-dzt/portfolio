/node_modules
# sem âncora: o build já foi gerado dentro de src/ por engano uma vez, e
# `/.next/` só cobre a raiz
.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
next-env.d.ts
